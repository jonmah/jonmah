import styled from '@emotion/styled'

export const LogoBox = styled.span`
  align-items: center;
  display: inline-flex;
  font-weight: bold;
  font-size: 18px;
  height: 30px;
  line-height: 20px;
  padding: 10px;

  &:hover img {
    transform: rotate(20deg);
  }
`
